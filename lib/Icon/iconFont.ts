interface IconFont {    config: string;}const customCache = new Set<string>();const creatIconFont = (arg: IconFont): void => {    const {config} = arg;    if (        !customCache.has(config) &&        config.length    ) {        const script: HTMLElement = document.createElement('script');        script.setAttribute('src', config);        script.setAttribute('data-namespace', config);        customCache.add(config);        document.body.appendChild(script);    }};export default creatIconFont;