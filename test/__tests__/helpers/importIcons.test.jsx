import Icon from '../../../lib/Icon/icon'import React from 'react'import { mount } from "enzyme"import "../../../lib/helpers/importIcons"import { importAll } from '../../../lib/helpers/importIcons'describe('importIcons',()=>{  it('引入本地icons',()=>{    const c = mount(<Icon name={'alipay'}/>)    const path = c.find('svg').find('use').find('path')    expect(path).not.toBeNull()  })  it('importAll',()=>{     const fn = (item,index) => {}     fn.constructor.prototype.keys = function(){       return [1,2,3]     }    expect(importAll(fn)).toBe(undefined)  })})